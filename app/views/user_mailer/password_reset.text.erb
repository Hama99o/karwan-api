<%= @body_text %>

<%# The code on its own line, because the body copy does not have to contain
    `%{code}` — Hamma9900 may paste Pashto that reads better without an inline
    number, and a plain-text email that carries no code would be useless. %>
<%= @code %>
